version: "3.9"

services:
  api:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'placeholder api service' && sleep infinity"]
    ports:
      - "3000:3000"

  web:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'placeholder web service' && sleep infinity"]
    ports:
      - "5173:5173"

  worker:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'placeholder worker service' && sleep infinity"]
